# Source registry

`sources/manifest.json` lists the first-party pages this pack uses as canonical references. `source-status.json` stores the most recent automated reachability/fingerprint check.

A fingerprint change is **not automatically a factual change**. It is a review signal. Website chrome, navigation, or formatting can change without altering product behavior.
