@AGENTS.md

## Claude Code notes

- For routing or description changes, measure with `claude plugin eval` (commands in README) and compare against the previous results before claiming an improvement.
- This repo root is also the Claude plugin (`.claude-plugin/`). Validate manifests with `claude plugin validate .`.
